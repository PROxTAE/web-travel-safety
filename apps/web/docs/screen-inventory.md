# Screen inventory (assets/ui-screens → routes)

Reference viewport 1672×941. Every value shown comes from the public API; the client never derives risk or action.

| # | Screen | Route | Visible elements | Data source | States covered |
| --- | --- | --- | --- | --- | --- |
| 01 | Dashboard overview | `/dashboard` | shell (sidebar/header/greeting), Weather/Transport/Risk summary cards → side sheets, My Trip card (origin/destination/depart/arrive est./transport/weather), Route & Risk map (routes + alert markers, legend, "Safer route found" chip), AI Recommendation card ("Use safer route" → compare), Emergency Help (SOS + directory numbers), Travel Assistant teaser (composer → new conversation) | `GET /me`, `GET /trips`, `GET /recommendations/{latest}`, `GET /emergency/contacts?country_code=` | loading skeletons, no-trip empty state, API error + retry, degraded banner, offline banner |
| 02 | Trip planner | `/trips/new`, `/trips/{id}` | Trip Details form (From/To autocomplete, Departure/Return in trip TZ, travel mode toggles, preferences, "Find safe routes"), Route Preview map (confirm pins, legend), assessment progress (SSE stages, cancel, needs-input resume), Route Options (Recommended/Fastest/Lowest risk or honest "unavailable"), recommendation card, mascot | `GET /locations/search`, `POST/PATCH /trips` (ETag), `POST /trips/{id}/assessments` (Idempotency-Key), `GET /runs/{id}/events` (SSE), `GET /runs/{id}`, `GET /recommendations/{id}` | validation errors (unconfirmed pin, past date, bad tz), 412 stale revision, agent outage, provider degraded, cancel |
| 03 | Global safety map | `/safety-map?layers=&t=` | header + legend (Low/Moderate/High/Unknown), Risk Layers panel (4 switches), MapLibre map (event markers by severity shape+colour, polygons, current route overlay), event card (severity, updated, View details, Avoid area), timeline Now/6h/12h, status chips (count, freshness, degraded) | `GET /safety/events?bbox&lookback_days&layers`, `POST /trips/{id}/assessments` (avoid area) | 0 events, provider degraded, request cancelled on viewport change, layers off hint |
| 04 | AI assistant | `/assistant/{conversationId}` | Recent chats + New chat, message list (sanitized markdown, progress bubble, recommendation blocks), quick chips (Check my route / Weather forecast / Emergency help), composer, Trip context (mini map, Risk/Weather/Transport rows, Use live location switch with explainer, Update my trip) | `GET/POST /conversations`, `GET/POST /conversations/{id}/messages`, SSE, `GET /recommendations/{id}`, `POST /consents` | new-chat auto-create, no-trip hint, 422 (unlinked chat), disconnect/reconnect, permission denied |
| 05 | Emergency center | `/emergency` | Hold-for-SOS (3 s hold, ring progress, keyboard) → Confirm dialog → Share location (consent + browser permission) → Connect help (verified numbers, tel: on tap), step indicator, live location map + Share/Stop, manual location fallback, mascot, Emergency profile (medical/contacts/insurance, Review details → encrypted save), In an emergency tips, Find nearest ×3 (police/medical/embassy) | `GET /emergency/contacts`, `GET /emergency/nearby`, `GET/PUT /me/emergency-profile`, `POST /consents` | permission denied → manual search, directory missing/limitations, nearby provider unavailable, early release cancels hold |
| 06 | Route comparison | `/trips/{id}/compare?recommendation=&candidate=` | header + "+N min · risk" summary, Original route column (duration/distance/stops, risk badge, exposure, Keep original + risk acknowledgement checkbox when MEDIUM/HIGH/AVOID), overlay map with legend, Safer route column (Recommended badge, Apply safer route, mascot), auto-update notice + Notify me switch (consent + subscription), sources | `GET /trips/{id}`, `GET /recommendations/{id}`, `POST /trips/{id}/apply-route` (If-Match + Idempotency-Key), `POST /consents`, `POST/DELETE /alert-subscriptions` | no alternative returned, 412 concurrent change, AVOID needs acknowledgement (server-validated), subscription failure rollback |

Shared: sidebar (desktop rail / mobile bottom nav), header profile menu (sign out), offline banner, toast region, skip link, error boundary with digest.
