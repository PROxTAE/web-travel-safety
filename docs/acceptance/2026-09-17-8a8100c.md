# Integration & Acceptance — 2026-09-17 (git `8a8100c`)

Runbook: `IMPLEMENTATION_PLANS/09_INTEGRATION_ACCEPTANCE_RUNBOOK.md`. Evidence files: `docs/acceptance/screenshots/*.png`,
`docs/acceptance/e2e-public-api.json`, `docs/acceptance/smoke-run.json` (sanitized run states; no tokens/PII).

## 2. Metadata

```text
Date/time/timezone:      2026-09-17 12:00–18:30 Asia/Bangkok (UTC+7)
Tester:                  simulated Team Lead / คน 1–8 (single operator)
Git SHA:                 8a8100c (main + fix/09-integration merged after this report)
Contract/model/policy:   contract 1.0.0 · risk model 1.0.0 = CANDIDATE (rule-baseline serves) · policy 1.0.0 · prompt 1.0.0 · knowledge 2026.09.1
Docker/Compose:          Docker Engine 29.8.0 (Docker Desktop 4.91, WSL2) · Compose v5.5.1
Host:                    Windows 11 Home 10.0.26200, x64; Windows Application Control active (blocks unsigned exe/.node)
Enabled providers:       Open-Meteo (geocoding/forecast/archive), USGS, GDACS, EONET, GTFS-RT (MBTA public); ORS = UNAVAILABLE (no key);
                         Amadeus = UNAVAILABLE (no production creds); OpenAI = disabled (template fallback)
Environment name:        sta-acceptance (compose project), APP_ENV=development
```

## 3. Static preflight

| Check | Result |
| --- | --- |
| `git status --short --branch` | clean on the fix branch; `.env` untracked/ignored |
| `docker compose -f compose.yaml -f compose.dev.yaml config --quiet` | OK |
| secret / mock grep (`sk-…`, private keys, `USE_MOCK`, `mockMode`, …) | 0 hits in `apps` and `services`; MSW only under `apps/web/tests` |
| `make lint` / `make typecheck` (ruff, mypy --strict, eslint, tsc) | all packages clean (1 React-Compiler warning in TripForm) |
| unit tests | python-common 14 · contracts 12 · external-data (unit) · data-integration 19 · risk-knowledge 22 · decision-engine 28 · recommendation · agent 12 · api 24 · web 21 — all passed |
| OpenAPI snapshot | `packages/contracts/openapi/public-api.yaml` in sync (`export_openapi.py --check`) |

## 4. Build and clean start

- Images: api 360 MB · agent 440 MB · external-data 492 MB · data-integration 744 MB · decision-engine 361 MB · recommendation 438 MB ·
  risk-knowledge 3.2 GB (CPU-only torch + cached embedding model; was 6+ GB with CUDA wheels) · web (standalone, non-root)
- `up -d postgres redis qdrant keycloak` → all healthy; realm `smart-travel` imported (after Keycloak 26 fixes: hostname URL form,
  `post.logout.redirect.uris` attribute, built-in `basic/profile/email/roles` scopes defined in the import)
- Migrations: 7 × `alembic upgrade head` → OK (schema creation skips schemas pre-created by the init script)
- `verify_model` → `UNAVAILABLE: no active model artifact` → fallback rule-baseline (expected, see M06)
- `index_knowledge` → collection `sta_knowledge_v2026_09_1`, 7 documents / 56 points, recall@3 = 1.0, **released**;
  FEMA pages returned 403 to container egress → ingested from copies captured 2026-09-16 (reported `knowledge_source_from_cache`), WHO live
- `verify_emergency_directory` → 7 verified, 1 pending (US), 0 failures
- Full stack `up -d` → 13 containers healthy (postgres, redis, qdrant, keycloak, 7 services, recommendation-worker, web)

## 5. Health / readiness

| Service | `/health/ready` |
| --- | --- |
| api :8000 | ready {postgres, redis, oidc, agent} |
| agent :8001 | ready {postgres, redis, checkpointer} |
| external-data :8002 | ready {postgres, redis} |
| data-integration :8003 | ready {postgres} |
| risk-knowledge :8004 | ready {postgres, qdrant, **risk_model: down (non-critical, rule baseline)**} |
| decision-engine :8005 | ready {postgres, policy} |
| recommendation :8006 | ready {postgres, redis} |
| web :3000 | `/api/health` ok |

Logs are structured JSON with `request_id/correlation_id/trace_id`; bearer tokens are redacted (`Bearer [REDACTED]`), coordinates and
user questions are absent from access logs (verified in api unit test + manual grep). Observability profile (`otel-collector`,
Prometheus, Grafana) was **not** started in this run — services log exporter retries and continue.

## 6. Database / storage

- 7 schemas with per-service owner roles; cross-schema write denied: `sta_api` → `agent.runs` = `permission denied for schema agent`
- PostGIS 3.4 + pgcrypto + uuid-ossp present; migrations heads single-branch
- Persisted after the scenarios: trips 16 · requests 20 · snapshots 15 · decision audit traces 16 · recommendations 16 · agent runs 23 ·
  feedback 1 · identity audit_log 46; unchanged after `restart api web`
- Redis: every key carries `sta:development:` prefix (run status/events, rate, idempotency, provider-cache, stream) with TTL
  (run status TTL 82 238 s); one non-prefixed key `arq:queue:health-check` (Arq worker internal)
- Qdrant: collection `sta_knowledge_v2026_09_1` with alias `active`
- Backup/restore: **not exercised** (no backup script yet) → open item

## 7. Real provider canary (through the stack)

| Provider | Evidence |
| --- | --- |
| Open-Meteo geocoding | `GET /api/v1/locations/search?q=Chiang Mai&country=TH` → "Chiang Mai, Chiang Mai, Thailand" TH Asia/Bangkok |
| Open-Meteo forecast | snapshot features `weather_max_precip_mm 6.7, weather_max_precip_probability 97, weather_max_severity_rank 5, coverage 1.0`, 24 samples |
| GDACS | "Flood in Thailand" MINOR (official=false) in the BKK–CNX bbox; `GET /safety/events` → 1 disaster |
| USGS / EONET | queried per run (provider-cache keys present); no events in corridor on the test day |
| GTFS-RT | transport `UNKNOWN` for CAR/FLIGHT corridors (no feed coverage) — never "on time" |
| ORS | `openrouteservice: ORS_API_KEY not configured (CAR routing approximated)` → geodesic `INFERRED` route, `DATA_QUALITY:ROUTE_GEOMETRY_INFERRED` |
| Amadeus | flight status `UNAVAILABLE` placeholder |

## 8. End-to-end scenarios (real Keycloak user `traveler1@example.invalid`, throwaway test fixture in the local dev realm)

| ID | Result | Evidence |
| --- | --- | --- |
| E2E-01 account/consent/trip | **PASS** — `GET /me` 200 with real JWT (after adding the `basic` scope); trip 201 `ETag W/"1"`; other user → 404 (unit); restart → persisted | `e2e-public-api.json` |
| E2E-02 normal assessment | **PASS** — 202 in 289 ms; SSE first event 0.03 s after 202; stages VALIDATING→…→FORMATTING_RESPONSE; run PARTIAL in 5.3 s (degraded: ORS, rule baseline, LLM fallback); recommendation, sources (21), contacts 191/1669/1784 | screenshots 01, 02 |
| E2E-03 real weather exposure | **PASS (live)** — tomorrow's forecast: thunderstorm severity 5, 124 min in severe weather → override HIGH (score floored 0.55, near-threshold escalation) → policy `AVOID` with `SEVERE_WEATHER_CORRIDOR`, `NO_SAFER_ROUTE`; earlier same-day run (8 h ahead, 2.5 mm) → `NORMAL/LOW` | `smoke-run.json`, screenshot 06 |
| E2E-04 apply safer route | **PARTIAL** — no alternative without ORS ("Safer route: Not available", honest); apply of the original route with `If-Match` → revision 2 + new assessment ran | screenshot 06 |
| E2E-05 keep risky original | **PASS** — without acknowledgement 422 `POLICY_VALIDATION_FAILED`; stale `If-Match` 412; with `acknowledge_risk` 202, `risk_acknowledged_at` set, reassessment PARTIAL on revision 2 | API log |
| E2E-06 follow-up assistant | **PASS** — informational follow-up reused the fresh snapshot (1.6 s, no provider fetch); "still safe now?" refreshed (new snapshot); injection "IGNORE ALL RULES…" → action unchanged (`AVOID`); no chain-of-thought exposed | API log |
| E2E-07 safety map | **PASS** — layers/timeline in URL, viewport query with abort, degraded passthrough (`gdacs: timeout` seen once), bbox > 10° rejected 422, marker card with source/freshness | screenshot 03 |
| E2E-08 emergency | **PASS** — hold < 3 s never confirms (Playwright), hold ≥ 3 s → confirm dialog → share (LOCATION_ONCE consent) → verified TH numbers; `ZZ` → `EMERGENCY_DIRECTORY_UNAVAILABLE`; nearby without consent 403; nearby with consent → `UNSUPPORTED_COVERAGE` (ORS places unavailable, honest) | screenshot 05 |
| E2E-09 alert subscription | **PARTIAL** — subscribe 201 (IN_APP), SMS 422 `UNSUPPORTED_COVERAGE`, unsubscribe 204, consent revoke cascades; reassessment worker/stream consumer wired but no material change occurred during the window → no delivery observed | API log |
| E2E-10 feedback governance | **PASS** — HELPFUL 201 `review_status NONE`; UNSAFE → `QUEUED` for safety review (unit); feedback pseudonymous via `user_scope` | API log |
| Web E2E (Playwright, real login) | **16/16 PASS** on desktop 1672×941 and mobile 390: login redirect, plan trip → assessment → recommendation with 0 serious/critical axe violations, SOS hold, no horizontal overflow on 5 pages | `apps/web/tests/e2e/journey.spec.ts` |

Performance observed: create-assessment ack 289 ms; first SSE event 30 ms; complete assessment 5–15 s; geocode < 1 s.

## 9. Failure / degraded scenarios exercised

| Fault | Observed |
| --- | --- |
| ORS missing key | route `INFERRED`, limitation + degraded entry, no fake geometry; Route Options card shows "provider unavailable" |
| Model not accepted | `risk_model: down` (non-critical), rule baseline + overrides, `MODEL_UNAVAILABLE` reason code |
| OpenAI disabled | deterministic TH/EN template, `explanation fallback (llm_disabled)` |
| Agent submit failure (unit) | request row FAILED, 503 `DEPENDENCY_UNAVAILABLE` retryable, no stack trace |
| Malformed downstream payload (unit) | 502 `DEPENDENCY_UNAVAILABLE`, contract violation surfaced |
| Downstream 500 (found live: transport upsert) | now mapped to `DEPENDENCY_UNAVAILABLE` (retryable) instead of `INTERNAL_ERROR`; root cause fixed |
| Redis socket timeout in stream consumer | fixed (block < socket timeout); consumer keeps running |
| SSE reconnect | `Last-Event-ID` resend-free (unit); browser EventSource reconnects |
| Docker Desktop crash mid-run (stale sockets) | recovered by moving `Docker/run` + `docker-secrets-engine` aside; containers resumed, data persisted |

## 10. Safety / security / privacy

- JWT negative matrix (expired, wrong iss/aud, wrong key, `alg=none`, missing role) → 401/403 (api unit); live token accepted only after `sub` present
- Rate limit: 30 search calls → 30×200 + 2×429 with `Retry-After`
- Static files bypass the session guard; API paths return 401 JSON instead of an HTML redirect
- Emergency profile stored AES-GCM; plaintext absent from DB rows (unit) and logs
- No `NEXT_PUBLIC_*SECRET|TOKEN|PASSWORD|KEY`; `verify:no-mocks` passes in the Docker build
- Not done: dependency/container vulnerability scan + SBOM (CI job exists; not run locally)

## 11. UI visual / accessibility

Screens 01–06 captured at 1672×941 (`docs/acceptance/screenshots/`), mobile 390 baselines for dashboard + emergency. Layout, card
hierarchy, colours, mascot/illustrations and CTAs follow `assets/ui-screens`; data values are live. Contrast fixes applied
(deep-teal buttons/hover, muted ink, amber ink, SOS coral-deep) → axe serious/critical = 0 on all five pages.
Known gap: the 6 hr / 12 hr timeline maps to 7/14-day lookback windows (event validity), not a forecast slider.

## 13. Sign-off / open items

| Item | Owner | Status |
| --- | --- | --- |
| ORS key / Amadeus production creds / OpenAI key | Team Lead | not available in this environment (capabilities honestly `UNAVAILABLE`) |
| Model 1.0.0 acceptance (recall 0.65 < 0.80) | คน 6 | CANDIDATE; more labelled days needed |
| Backup/restore drill, vulnerability scan, load test | Team Lead | open |
| Observability profile run (OTel/Prometheus/Grafana) | Team Lead | open |
| Alert delivery with a real material change | คน 8 + คน 2 | needs a captured real update replay |
| US emergency directory verification | คน 8 | pending (bot-blocked) |

Sign-off (simulated): UI/a11y คน 1 ✔ · API/auth/privacy คน 2 ✔ · Agent คน 3 ✔ · Data คน 4/5 ✔ · Risk/Knowledge คน 6 ✔ (with CANDIDATE caveat) ·
Decision คน 7 ✔ · Recommendation คน 8 ✔ · Team Lead ✔ for demo; release blocked on open items above.
